class Solution(object):
    def findGCD(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        gcd=[]
        maxn=max(nums)
        minn=min(nums)
        
        for i in range (1,maxn+1):
                if maxn%i==0 and minn%i==0:
                    gcd.append(i)
        
        maxgcd=gcd[0]
        for i in gcd:
              if i>=maxgcd:
                maxgcd=i
        return maxgcd
        
